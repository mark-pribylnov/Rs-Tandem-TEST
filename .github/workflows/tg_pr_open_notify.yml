name: PR Notification
on:
  pull_request:
    types: [opened]

jobs:
  node-message:
    runs-on: ubuntu-latest
    steps:
      - name: send telegram notification
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: html
          message: |
            <b>🚀 Открыт новый Pull Request!</b>
            
            <b>Название:</b> ${{ github.event.pull_request.title }}
            <b>Автор:</b> ${{ github.actor }}
            <b>Ветка:</b> ${{ github.head_ref }} -> ${{ github.base_ref }}
            
            <a href="${{ github.event.pull_request.html_url }}">Посмотреть PR на GitHub!</a>
